// Declaro tres variables y le pido al usuario que ingrese dos numeros, se los asigno a dos de las variables

let numero1 = Number (prompt ('Por favor ingresa un primer número: '));
let numero2 = Number (prompt ('Por favor ingresa un segundo número, para poder sumar ambos números ingresados: '));

// Nombro a la tercer variable y le asigno el valor de la suma de ambos números ingresados por el usuario
let numero3 = numero1 + numero2 

// Muestro por alerta el valor de la suma
alert (`El resultado es: ${numero3}`);
